# ─── OpenAI ────────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o          # or gpt-4o-mini for lower cost

# ─── Google OAuth2 (one set of credentials for both accounts) ──────────────
# Create credentials at: https://console.cloud.google.com/apis/credentials
# Type: OAuth 2.0 Client ID → Desktop App
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=urn:ietf:wg:oauth:2.0:oob

# ─── Gmail Account Aliases ─────────────────────────────────────────────────
# These match the token file names in tokens/<alias>.token.json
# Run: npm run auth -- personal   (then npm run auth -- work)
GMAIL_ACCOUNT_1_ALIAS=personal
GMAIL_ACCOUNT_2_ALIAS=work

# ─── News API (GNews) ──────────────────────────────────────────────────────
# Get a free key at: https://gnews.io
NEWS_API_KEY=your-gnews-api-key

# Comma-separated topics for your morning news briefing
NEWS_TOPICS=Artificial Intelligence,AWS Cloud,Florida real estate market

# ─── Telegram Bot (optional — for mobile chat) ─────────────────────────────
# Create a bot with @BotFather on Telegram to get your token
TELEGRAM_TOKEN=your-telegram-bot-token

# Your personal Telegram chat ID (prevents others from using your bot)
# Get it by messaging @userinfobot on Telegram
TELEGRAM_AUTHORIZED_CHAT_ID=123456789

# ─── Twilio (WhatsApp + Voice) ────────────────────────────────────────────
# Sign up at: https://www.twilio.com
# WhatsApp sandbox: https://console.twilio.com/us1/develop/sms/try-it-out/whatsapp-learn
# Voice number: buy a phone number and point its webhook to your server
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886   # Twilio sandbox number

# Your personal WhatsApp number in E.164 format (blocks others from using the bot)
AUTHORIZED_WHATSAPP_NUMBER=whatsapp:+15551234567

# Public HTTPS URL where this server is reachable (required for Twilio webhooks)
# Use ngrok for local dev: ngrok http 3000
PUBLIC_URL=https://your-server.example.com

# ─── Timezone ──────────────────────────────────────────────────────────────
# Used for calendar event creation — find yours at: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TIMEZONE=America/New_York

# ─── AWS (for EC2 + SSM token storage) ──────────────────────────────────────────────────────────────
# Set USE_SSM=true on EC2 to read OAuth tokens from Parameter Store instead of files
USE_SSM=false
AWS_REGION=us-east-1
# SSM path prefix for token storage (tokens saved as /daily-planner/tokens/<alias>)
SSM_PREFIX=/daily-planner/tokens
# On EC2 with an IAM role attached, no keys needed. For local SSM upload:
# AWS_ACCESS_KEY_ID=your-key
# AWS_SECRET_ACCESS_KEY=your-secret

# ─── Server ────────────────────────────────────────────────────────────────────────────────────────────────────────────────
PORT=3000
