# dependencies
node_modules/

# build output — included for EC2 deploy (no build step on server)
# dist/

# environment variables — NEVER commit these
.env

# OAuth tokens — NEVER commit these (contain your account access)
tokens/

# logs
*.log
npm-debug.log*
logs/*.log
logs/*.gz

# keep the logs directory itself
!logs/.gitkeep
